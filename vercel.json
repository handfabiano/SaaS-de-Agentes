{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "cd painel && npm install",
  "buildCommand": "cd painel && npm run build",
  "outputDirectory": "painel/dist",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
